<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gravens Arv - Kampanjdashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --flame-orange: #ff6b4a;
            --deep-red: #d4534d;
            --mystic-purple: #a855a8;
            --shadow-purple: #8b4789;
            --gold: #ffa500;
            --dark-gold: #d4af37;
            --bg-dark: #0a0a0a;
            --bg-darker: #050505;
            --bg-card: #1a1a1a;
            --bg-hover: #2a2a2a;
            --text-primary: #f5f5f5;
            --text-secondary: #a0a0a0;
            --border: #333;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: var(--bg-dark);
            color: var(--text-primary);
            line-height: 1.6;
        }

        /* Hero Section */
        .hero {
            height: 400px;
            background: linear-gradient(rgba(0,0,0,0.5), rgba(10,10,10,0.9)), url('dashboard/hero-bg.png') center/cover;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            border-bottom: 3px solid var(--flame-orange);
            position: relative;
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, var(--flame-orange), var(--gold));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--text-secondary);
            max-width: 700px;
        }

        /* Stats Cards */
        .stats-container {
            max-width: 1400px;
            margin: -60px auto 2rem;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            position: relative;
            z-index: 10;
        }

        .stat-card {
            background: linear-gradient(135deg, var(--bg-card), var(--bg-darker));
            padding: 1.5rem;
            border-radius: 8px;
            border: 1px solid var(--border);
            border-left: 4px solid var(--flame-orange);
        }

        .stat-card.purple { border-left-color: var(--mystic-purple); }
        .stat-card.gold { border-left-color: var(--gold); }
        .stat-card.red { border-left-color: var(--deep-red); }

        .stat-label {
            font-size: 0.9rem;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .stat-value {
            font-size: 2.5rem;
            font-weight: bold;
            margin: 0.5rem 0;
            background: linear-gradient(135deg, var(--flame-orange), var(--gold));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .stat-card.purple .stat-value {
            background: linear-gradient(135deg, var(--mystic-purple), var(--shadow-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .stat-card.gold .stat-value {
            background: linear-gradient(135deg, var(--gold), var(--dark-gold));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .stat-card.red .stat-value {
            background: linear-gradient(135deg, var(--deep-red), var(--flame-orange));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* Navigation */
        .nav {
            max-width: 1400px;
            margin: 2rem auto;
            padding: 0 2rem;
            display: flex;
            gap: 1rem;
            border-bottom: 2px solid var(--border);
            overflow-x: auto;
        }

        .nav-btn {
            background: none;
            border: none;
            color: var(--text-secondary);
            padding: 1rem 1.5rem;
            cursor: pointer;
            font-size: 1rem;
            transition: all 0.3s;
            border-bottom: 3px solid transparent;
            white-space: nowrap;
        }

        .nav-btn:hover {
            color: var(--text-primary);
            background: var(--bg-hover);
        }

        .nav-btn.active {
            color: var(--flame-orange);
            border-bottom-color: var(--flame-orange);
        }

        /* Main Content */
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }

        .section {
            display: none;
        }

        .section.active {
            display: block;
        }

        /* Search & Filter Bar */
        .search-filter-bar {
            margin-bottom: 2rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .search-box {
            position: relative;
        }

        .search-box input {
            width: 100%;
            padding: 1rem 1rem 1rem 3rem;
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 8px;
            color: var(--text-primary);
            font-size: 1rem;
        }

        .search-box::before {
            content: "🔍";
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
        }

        .filter-tags {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .filter-tag {
            padding: 0.5rem 1rem;
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 20px;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 0.9rem;
        }

        .filter-tag:hover {
            background: var(--bg-hover);
        }

        .filter-tag.active {
            background: var(--flame-orange);
            border-color: var(--flame-orange);
            color: white;
        }

        .filter-section {
            margin-bottom: 1rem;
        }

        .filter-section h4 {
            color: var(--gold);
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
            text-transform: uppercase;
        }

        /* Cards Grid */
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 1.5rem;
        }

        .card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 1.5rem;
            transition: all 0.3s;
            cursor: pointer;
            position: relative;
        }

        .card:hover {
            background: var(--bg-hover);
            border-color: var(--flame-orange);
            transform: translateY(-2px);
        }

        .card h3 {
            color: var(--gold);
            margin-bottom: 0.75rem;
            font-size: 1.2rem;
        }

        .card-meta {
            display: flex;
            gap: 0.5rem;
            margin-bottom: 0.75rem;
            flex-wrap: wrap;
        }

        .tag {
            background: rgba(255, 107, 74, 0.2);
            color: var(--flame-orange);
            padding: 0.25rem 0.75rem;
            border-radius: 12px;
            font-size: 0.85rem;
            border: 1px solid rgba(255, 107, 74, 0.3);
        }

        .tag.purple {
            background: rgba(168, 85, 168, 0.2);
            color: var(--mystic-purple);
            border-color: rgba(168, 85, 168, 0.3);
        }

        .tag.gold {
            background: rgba(255, 165, 0, 0.2);
            color: var(--gold);
            border-color: rgba(255, 165, 0, 0.3);
        }

        .tag.green {
            background: rgba(46, 213, 115, 0.2);
            color: #2ed573;
            border-color: rgba(46, 213, 115, 0.3);
        }

        .tag.red {
            background: rgba(212, 83, 77, 0.2);
            color: var(--deep-red);
            border-color: rgba(212, 83, 77, 0.3);
        }

        .card-info {
            color: var(--text-secondary);
            font-size: 0.95rem;
            line-height: 1.5;
        }

        /* Card Image Preview */
        .card-image-preview {
            position: absolute;
            top: 1rem;
            right: 1rem;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid var(--gold);
            opacity: 0.9;
            transition: all 0.3s;
        }

        .card:hover .card-image-preview {
            opacity: 1;
            border-color: var(--flame-orange);
            transform: scale(1.1);
        }

        /* Modal */
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            overflow: auto;
        }

        .modal.active {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            background: var(--bg-card);
            border: 2px solid var(--flame-orange);
            border-radius: 12px;
            padding: 2rem;
            max-width: 800px;
            width: 90%;
            max-height: 80vh;
            overflow-y: auto;
            position: relative;
        }

        .modal-beskrivning {
            margin-top: 1.5rem;
            padding-top: 1.5rem;
            border-top: 2px solid var(--border);
            color: var(--text-primary);
            line-height: 1.8;
        }

        .modal-beskrivning h1,
        .modal-beskrivning h2,
        .modal-beskrivning h3 {
            color: var(--gold);
            margin-top: 1rem;
            margin-bottom: 0.5rem;
        }

        .modal-beskrivning strong {
            color: var(--flame-orange);
        }

        .modal-beskrivning p {
            margin-bottom: 1rem;
        }

        .modal-close {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: var(--deep-red);
            border: none;
            color: white;
            width: 35px;
            height: 35px;
            border-radius: 50%;
            cursor: pointer;
            font-size: 1.5rem;
            line-height: 1;
        }

        .modal-close:hover {
            background: var(--flame-orange);
        }

        .modal h2 {
            color: var(--gold);
            margin-bottom: 1rem;
            padding-right: 40px;
        }

        .modal-row {
            display: flex;
            justify-content: space-between;
            padding: 0.75rem 0;
            border-bottom: 1px solid var(--border);
        }

        .modal-row:last-child {
            border-bottom: none;
        }

        .modal-label {
            color: var(--text-secondary);
            font-weight: 500;
        }

        .modal-value {
            color: var(--text-primary);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2rem;
            }

            .stats-container {
                grid-template-columns: 1fr;
                margin-top: 2rem;
            }

            .nav {
                flex-wrap: nowrap;
                overflow-x: scroll;
            }

            .cards-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Special: Player Characters */
        .player-card {
            border-left: 4px solid var(--gold);
        }

        .player-card.forsvunnen {
            border-left-color: var(--deep-red);
            opacity: 0.8;
        }

        /* Timeline (for chapters) */
        .timeline {
            position: relative;
            padding-left: 2rem;
        }

        .timeline::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 3px;
            background: linear-gradient(180deg, var(--flame-orange), var(--mystic-purple));
        }

        .timeline-item {
            position: relative;
            margin-bottom: 2rem;
            padding-left: 2rem;
        }

        .timeline-item::before {
            content: '';
            position: absolute;
            left: -2.5rem;
            top: 0.5rem;
            width: 12px;
            height: 12px;
            background: var(--bg-card);
            border: 3px solid var(--gold);
            border-radius: 50%;
        }

        .timeline-item.current::before {
            background: var(--flame-orange);
            box-shadow: 0 0 20px var(--flame-orange);
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        .timeline-item h3 {
            color: var(--gold);
            margin-bottom: 0.5rem;
        }

        .timeline-item .meta {
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }

        /* Loading state */
        .loading {
            text-align: center;
            padding: 3rem;
            color: var(--text-secondary);
        }

        /* Lightbox */
        .lightbox {
            display: none;
            position: fixed;
            z-index: 2000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            align-items: center;
            justify-content: center;
            cursor: pointer;
        }

        .lightbox.active {
            display: flex;
        }

        .lightbox img {
            max-width: 90%;
            max-height: 90%;
            object-fit: contain;
            border: 3px solid var(--gold);
            border-radius: 8px;
            box-shadow: 0 0 50px rgba(255, 165, 0, 0.3);
        }

        .lightbox-close {
            position: absolute;
            top: 2rem;
            right: 2rem;
            background: var(--bg-card);
            border: 2px solid var(--gold);
            color: var(--text-primary);
            font-size: 2rem;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s;
        }

        .lightbox-close:hover {
            background: var(--flame-orange);
            border-color: var(--flame-orange);
            transform: rotate(90deg);
        }

        /* Modal Image Clickable */
        .modal-image-clickable {
            cursor: zoom-in;
            transition: all 0.3s;
        }

        .modal-image-clickable:hover {
            opacity: 0.9;
            transform: scale(1.02);
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <div class="hero">
        <h1>Gravens Arv</h1>
        <p>Kapitel 10: Skugglandet · Gruppen söker Zentri, fången av Lord VinterGlöd</p>
    </div>

    <!-- Stats Cards -->
    <div class="stats-container">
        <div class="stat-card">
            <div class="stat-label">Kapitel</div>
            <div class="stat-value">11</div>
            <div class="stat-label">Totalt</div>
        </div>

        <div class="stat-card purple">
            <div class="stat-label">Nuvarande</div>
            <div class="stat-value">Skugglandet</div>
            <div class="stat-label">Kapitel 10</div>
        </div>

        <div class="stat-card gold">
            <div class="stat-label">Spelarkaraktärer</div>
            <div class="stat-value">6</div>
            <div class="stat-label">Inklusive Zentri</div>
        </div>

        <div class="stat-card red">
            <div class="stat-label">NPCs</div>
            <div class="stat-value" id="npc-count">205</div>
            <div class="stat-label">Katalogiserade</div>
        </div>
    </div>

    <!-- Navigation -->
    <div class="nav">
        <button class="nav-btn active" onclick="showSection('kapitel')">📜 Kapitel</button>
        <button class="nav-btn" onclick="showSection('players')">⚔️ Spelarkaraktärer</button>
        <button class="nav-btn" onclick="showSection('npcs')">👥 NPCs</button>
        <button class="nav-btn" onclick="showSection('platser')">🏰 Platser</button>
        <button class="nav-btn" onclick="showSection('fraktioner')">⚜️ Fraktioner</button>
    </div>

    <!-- Main Content -->
    <div class="container">
        <!-- Kapitel Section -->
        <div id="kapitel" class="section active">
            <h2 style="color: var(--gold); margin-bottom: 1.5rem;">Kampanjhistorik</h2>
            <div class="timeline" id="timeline-container"></div>
        </div>

        <!-- Players Section -->
        <div id="players" class="section">
            <h2 style="color: var(--gold); margin-bottom: 1.5rem;">Spelarkaraktärer</h2>
            <div class="cards-grid" id="players-container"></div>
        </div>

        <!-- NPCs Section -->
        <div id="npcs" class="section">
            <h2 style="color: var(--gold); margin-bottom: 1.5rem;">NPCs</h2>

            <div class="search-filter-bar">
                <div class="search-box">
                    <input type="text" id="npc-search" placeholder="Sök NPCs...">
                </div>

                <div class="filter-section">
                    <h4>Region</h4>
                    <div class="filter-tags" id="region-filters"></div>
                </div>

                <div class="filter-section">
                    <h4>Status</h4>
                    <div class="filter-tags" id="status-filters"></div>
                </div>

                <div class="filter-section">
                    <h4>Ras</h4>
                    <div class="filter-tags" id="ras-filters"></div>
                </div>
            </div>

            <div class="cards-grid" id="npcs-container"></div>
        </div>

        <!-- Platser Section -->
        <div id="platser" class="section">
            <h2 style="color: var(--gold); margin-bottom: 1.5rem;">Platser</h2>

            <div class="search-filter-bar">
                <div class="search-box">
                    <input type="text" id="plats-search" placeholder="Sök platser...">
                </div>

                <div class="filter-section">
                    <h4>Region</h4>
                    <div class="filter-tags" id="plats-region-filters"></div>
                </div>

                <div class="filter-section">
                    <h4>Typ</h4>
                    <div class="filter-tags" id="typ-filters"></div>
                </div>
            </div>

            <div class="cards-grid" id="platser-container"></div>
        </div>

        <!-- Fraktioner Section -->
        <div id="fraktioner" class="section">
            <h2 style="color: var(--gold); margin-bottom: 1.5rem;">Fraktioner</h2>
            <div class="cards-grid" id="fraktioner-container"></div>
        </div>
    </div>

    <!-- Modal -->
    <div id="modal" class="modal">
        <div class="modal-content">
            <button class="modal-close" onclick="closeModal()">×</button>
            <div id="modal-body"></div>
        </div>
    </div>

    <!-- Lightbox for full-size images -->
    <div id="lightbox" class="lightbox" onclick="closeLightbox()">
        <button class="lightbox-close" onclick="closeLightbox()">×</button>
        <img id="lightbox-image" src="" alt="Full size image">
    </div>

    <script src="./wiki_data.js"></script>
    <script>
        // Data
        let allNpcs = [];
        let allPlatser = [];
        let filteredNpcs = [];
        let filteredPlatser = [];
        let activeFilters = {
            region: [],
            status: [],
            ras: [],
            typ: []
        };

        // Load data
        function init() {
            if (typeof wikiData === 'undefined') {
                document.getElementById('npcs-container').innerHTML = '<div class="loading">Laddar data...</div>';
                return;
            }

            allNpcs = wikiData.npcs || [];
            allPlatser = wikiData.platser || [];
            filteredNpcs = [...allNpcs];
            filteredPlatser = [...allPlatser];

            renderKapitel();
            renderPlayers();
            renderNpcs();
            renderPlatser();
            renderFraktioner();
            setupFilters();
            setupSearch();
        }

        // Kapitel data
        const kapitel = [
            {nummer: "Prolog", titel: "Tirakgraven", plats: "Gränstrakterna Cermira/Mithera", status: "avslutad", beskrivning: "Gruppen vaknar fängslade i demonfursten Tiraks grav."},
            {nummer: 1, titel: "Jakten", plats: "Cermiras skogar → Grensfortet", status: "avslutad", beskrivning: "Jakten på Tamron/Ibran genom Cermiras skogar."},
            {nummer: 2, titel: "Muhad", plats: "Jarla → Jen, Muhad", status: "avslutad", beskrivning: "Uppdraget att hitta Hagge leder till Muhad."},
            {nummer: 3, titel: "Tarkas", plats: "Tarkas kust och ökenlandskap", status: "avslutad", beskrivning: "Skeppsbrottet och äventyr i Tarkas."},
            {nummer: 4, titel: "Jargien", plats: "Jargiska kejsardömet", status: "avslutad", beskrivning: "Genom Jargien mot hemmet."},
            {nummer: 5, titel: "Vargnäset", plats: "Byn Vargnäset", status: "avslutad", beskrivning: "Första besöket i Vargnäset, Zentris försvinnande."},
            {nummer: 6, titel: "Vitterdal", plats: "Vitterdal, Cermira", status: "avslutad", beskrivning: "Återkomst till Vitterdal och belöning."},
            {nummer: 7, titel: "Vargnäset igen", plats: "Vargnäset (ockuperat)", status: "avslutad", beskrivning: "Tillbaka till Vargnäset - nu ockuperat."},
            {nummer: 8, titel: "Evakueringen", plats: "Flykt genom Drunok", status: "avslutad", beskrivning: "Evakuering med 50 civila."},
            {nummer: 9, titel: "Mithera", plats: "Mithera-skogen", status: "avslutad", beskrivning: "Genom Mithera mot Skugglandet."},
            {nummer: 10, titel: "Skugglandet", plats: "Skugglandets förvrängda verklighet", status: "pågående", beskrivning: "Gruppen söker Zentri i Skugglandet."}
        ];

        // Players data
        const players = [
            {namn: "Gordon Nahrzezia", spelare: "Calle", ras: "Cirefalier", klass: "Legosoldat", alder: "~30 år", status: "Aktiv", plats: "Skugglandet"},
            {namn: "Thrakka Järnhanden", spelare: "Andreas", ras: "Tirak", klass: "Gladiator/Livvakt", status: "Aktiv", plats: "Skugglandet"},
            {namn: "Umnatak", spelare: "Christofer", ras: "Auser", klass: "Spejare/Schaman", status: "Aktiv", plats: "Skugglandet"},
            {namn: "Kazrik klan Ghor", spelare: "Jonas", ras: "Dvärg", klass: "Författare (\"Den resande\")", status: "Aktiv", plats: "Skugglandet"},
            {namn: "Corvus Askhar", spelare: "Daniel", ras: "Människa", klass: "F.d. Kommendör Arvorns Hammare", alder: "31 år", status: "Aktiv", plats: "Skugglandet"},
            {namn: "Zentri Bredarsson", spelare: "Andreas (f.d.)", ras: "Asharier", klass: "Köpmansson/Krigarmagiker", status: "Försvunnen", plats: "Skugglandet (fången)"}
        ];

        // Fraktioner data
        const fraktioner = [
            {namn: "Arvorns Hammare", status: "Fiende", beskrivning: "Häxjägare som jagar gruppen. Corvus gamla orden."},
            {namn: "Zorian-orden/Systrarna", status: "Komplex", beskrivning: "Jagar Zentri (demonen). Osäker relation till gruppen."},
            {namn: "Sanari-alverna", status: "Potentiell allierad", beskrivning: "Har bindningsföremålen mot demonen Urkhath."},
            {namn: "Dvärgarna (Klan Drezin)", status: "Potentiell allierad", beskrivning: "Har kunskap om demonologi. Kazrik kan vara nyckeln."},
            {namn: "Vitterdals hov", status: "Allierad", beskrivning: "Hagge och Lady Soffia. Gruppens beskyddare."},
            {namn: "Stålsvärdskavaleristerna", status: "Bakgrund", beskrivning: "Gruppens tidigare arbetsgivare."}
        ];

        function renderKapitel() {
            const container = document.getElementById('timeline-container');
            container.innerHTML = kapitel.map((k, i) => `
                <div class="timeline-item ${k.status === 'pågående' ? 'current' : ''}">
                    <h3>Kapitel ${k.nummer}: ${k.titel}</h3>
                    <div class="meta">${k.plats} | Status: ${k.status}</div>
                    <p>${k.beskrivning}</p>
                </div>
            `).join('');
        }

        function renderPlayers() {
            const container = document.getElementById('players-container');
            container.innerHTML = players.map(p => `
                <div class="card player-card ${p.status === 'Försvunnen' ? 'forsvunnen' : ''}" onclick='showPlayerModal(${JSON.stringify(p)})'>
                    <h3>${p.namn}</h3>
                    <div class="card-meta">
                        <span class="tag gold">${p.spelare}</span>
                        <span class="tag purple">${p.ras}</span>
                        <span class="tag ${p.status === 'Försvunnen' ? 'red' : 'green'}">${p.status}</span>
                    </div>
                    <div class="card-info">
                        <strong>${p.klass}</strong>${p.alder ? ` · ${p.alder}` : ''}<br>
                        📍 ${p.plats}
                    </div>
                </div>
            `).join('');
        }

        function renderNpcs() {
            const container = document.getElementById('npcs-container');
            const npcs = filteredNpcs;

            if (npcs.length === 0) {
                container.innerHTML = '<div class="loading">Inga NPCs matchade din sökning</div>';
                return;
            }

            container.innerHTML = npcs.map(npc => {
                const bildUrl = npc.bild ? `./kampanjwiki/assets/images/npcs/${npc.bild}` : null;
                return `
                <div class="card" onclick='showNpcModal(${JSON.stringify(npc).replace(/'/g, "&#39;")})'>
                    ${bildUrl ? `<img src="${bildUrl}" alt="${npc.namn}" class="card-image-preview">` : ''}
                    <h3>${npc.namn}</h3>
                    <div class="card-meta">
                        ${npc.ras ? `<span class="tag purple">${npc.ras}</span>` : ''}
                        ${npc.status ? `<span class="tag ${getStatusColor(npc.status)}">${npc.status}</span>` : ''}
                        ${npc.plats ? `<span class="tag gold">📍 ${npc.plats}</span>` : ''}
                    </div>
                    <div class="card-info">
                        ${npc.fraktion ? `⚔️ ${npc.fraktion}` : 'Ingen fraktion'}
                    </div>
                </div>
                `;
            }).join('');

            document.getElementById('npc-count').textContent = npcs.length;
        }

        function renderPlatser() {
            const container = document.getElementById('platser-container');
            const platser = filteredPlatser;

            if (platser.length === 0) {
                container.innerHTML = '<div class="loading">Inga platser matchade din sökning</div>';
                return;
            }

            container.innerHTML = platser.map(plats => `
                <div class="card" onclick='showPlatsModal(${JSON.stringify(plats).replace(/'/g, "&#39;")})'>
                    <h3>${plats.namn}</h3>
                    <div class="card-meta">
                        ${plats.typ ? `<span class="tag purple">${plats.typ}</span>` : ''}
                        ${plats.region ? `<span class="tag gold">📍 ${plats.region}</span>` : ''}
                    </div>
                    <div class="card-info">
                        ${plats.beskrivning ? plats.beskrivning.substring(0, 100) + '...' : ''}
                    </div>
                </div>
            `).join('');
        }

        function renderFraktioner() {
            const container = document.getElementById('fraktioner-container');
            container.innerHTML = fraktioner.map(f => `
                <div class="card">
                    <h3>${f.namn}</h3>
                    <div class="card-meta">
                        <span class="tag ${getFraktionColor(f.status)}">${f.status}</span>
                    </div>
                    <div class="card-info">
                        ${f.beskrivning}
                    </div>
                </div>
            `).join('');
        }

        function getStatusColor(status) {
            if (!status) return '';
            const s = status.toLowerCase();
            if (s.includes('död')) return 'red';
            if (s.includes('försvunnen')) return 'red';
            if (s.includes('levande')) return 'green';
            return '';
        }

        function getFraktionColor(status) {
            if (status === 'Fiende') return 'red';
            if (status === 'Allierad') return 'green';
            if (status === 'Potentiell allierad') return 'gold';
            return 'purple';
        }

        function setupFilters() {
            // Extract unique values
            const regioner = [...new Set(allNpcs.map(n => extractRegion(n.plats)).filter(r => r))];
            const statuses = [...new Set(allNpcs.map(n => n.status).filter(s => s))];
            const raser = [...new Set(allNpcs.map(n => n.ras).filter(r => r))];
            const typer = [...new Set(allPlatser.map(p => p.typ).filter(t => t))];
            const platsRegioner = [...new Set(allPlatser.map(p => p.region).filter(r => r))];

            // Render region filters
            document.getElementById('region-filters').innerHTML = regioner.sort().map(r =>
                `<span class="filter-tag" data-filter="region" data-value="${r}">${r}</span>`
            ).join('');

            // Render status filters
            document.getElementById('status-filters').innerHTML = statuses.sort().map(s =>
                `<span class="filter-tag" data-filter="status" data-value="${s}">${s}</span>`
            ).join('');

            // Render ras filters
            document.getElementById('ras-filters').innerHTML = raser.sort().map(r =>
                `<span class="filter-tag" data-filter="ras" data-value="${r}">${r}</span>`
            ).join('');

            // Render plats region filters
            document.getElementById('plats-region-filters').innerHTML = platsRegioner.sort().map(r =>
                `<span class="filter-tag" data-filter="plats-region" data-value="${r}">${r}</span>`
            ).join('');

            // Render typ filters
            document.getElementById('typ-filters').innerHTML = typer.sort().map(t =>
                `<span class="filter-tag" data-filter="typ" data-value="${t}">${t}</span>`
            ).join('');

            // Add click handlers
            document.querySelectorAll('.filter-tag').forEach(tag => {
                tag.addEventListener('click', handleFilterClick);
            });
        }

        function extractRegion(plats) {
            if (!plats) return null;
            if (plats.includes('Muhad') || plats.includes('Jen')) return 'Muhad';
            if (plats.includes('Cermira')) return 'Cermira';
            if (plats.includes('Mithera')) return 'Mithera';
            if (plats.includes('Tarkas')) return 'Tarkas';
            if (plats.includes('Skugglandet')) return 'Skugglandet';
            if (plats.includes('Vargnäset')) return 'Vargnäset';
            if (plats.includes('Vitterdal')) return 'Vitterdal';
            if (plats.includes('Tuzan Rim')) return 'Tuzan Rim';
            if (plats.includes('Jarla')) return 'Jarla';
            return 'Övriga';
        }

        function handleFilterClick(e) {
            const tag = e.target;
            const filterType = tag.dataset.filter;
            const value = tag.dataset.value;

            tag.classList.toggle('active');

            if (filterType === 'plats-region') {
                if (tag.classList.contains('active')) {
                    activeFilters.platsRegion = activeFilters.platsRegion || [];
                    activeFilters.platsRegion.push(value);
                } else {
                    activeFilters.platsRegion = (activeFilters.platsRegion || []).filter(v => v !== value);
                }
                applyPlatsFilters();
            } else if (filterType === 'typ') {
                if (tag.classList.contains('active')) {
                    activeFilters.typ = activeFilters.typ || [];
                    activeFilters.typ.push(value);
                } else {
                    activeFilters.typ = (activeFilters.typ || []).filter(v => v !== value);
                }
                applyPlatsFilters();
            } else {
                if (tag.classList.contains('active')) {
                    activeFilters[filterType].push(value);
                } else {
                    activeFilters[filterType] = activeFilters[filterType].filter(v => v !== value);
                }
                applyNpcFilters();
            }
        }

        function applyNpcFilters() {
            const searchQuery = document.getElementById('npc-search').value.toLowerCase();

            filteredNpcs = allNpcs.filter(npc => {
                // Search filter
                if (searchQuery && !npc.namn.toLowerCase().includes(searchQuery)) {
                    return false;
                }

                // Region filter
                if (activeFilters.region.length > 0) {
                    const npcRegion = extractRegion(npc.plats);
                    if (!activeFilters.region.includes(npcRegion)) return false;
                }

                // Status filter
                if (activeFilters.status.length > 0) {
                    if (!activeFilters.status.includes(npc.status)) return false;
                }

                // Ras filter
                if (activeFilters.ras.length > 0) {
                    if (!activeFilters.ras.includes(npc.ras)) return false;
                }

                return true;
            });

            renderNpcs();
        }

        function applyPlatsFilters() {
            const searchQuery = document.getElementById('plats-search').value.toLowerCase();

            filteredPlatser = allPlatser.filter(plats => {
                // Search filter
                if (searchQuery && !plats.namn.toLowerCase().includes(searchQuery)) {
                    return false;
                }

                // Region filter
                if (activeFilters.platsRegion && activeFilters.platsRegion.length > 0) {
                    if (!activeFilters.platsRegion.includes(plats.region)) return false;
                }

                // Typ filter
                if (activeFilters.typ && activeFilters.typ.length > 0) {
                    if (!activeFilters.typ.includes(plats.typ)) return false;
                }

                return true;
            });

            renderPlatser();
        }

        function setupSearch() {
            document.getElementById('npc-search').addEventListener('input', applyNpcFilters);
            document.getElementById('plats-search').addEventListener('input', applyPlatsFilters);
        }

        function showPlayerModal(player) {
            const modal = document.getElementById('modal');
            const body = document.getElementById('modal-body');

            body.innerHTML = `
                <h2>${player.namn}</h2>
                <div class="modal-row">
                    <span class="modal-label">Spelare:</span>
                    <span class="modal-value">${player.spelare}</span>
                </div>
                <div class="modal-row">
                    <span class="modal-label">Ras:</span>
                    <span class="modal-value">${player.ras}</span>
                </div>
                <div class="modal-row">
                    <span class="modal-label">Klass:</span>
                    <span class="modal-value">${player.klass}</span>
                </div>
                ${player.alder ? `<div class="modal-row">
                    <span class="modal-label">Ålder:</span>
                    <span class="modal-value">${player.alder}</span>
                </div>` : ''}
                <div class="modal-row">
                    <span class="modal-label">Status:</span>
                    <span class="modal-value">${player.status}</span>
                </div>
                <div class="modal-row">
                    <span class="modal-label">Plats:</span>
                    <span class="modal-value">${player.plats}</span>
                </div>
            `;

            modal.classList.add('active');
        }

        function showNpcModal(npc) {
            const modal = document.getElementById('modal');
            const body = document.getElementById('modal-body');

            // Convert markdown to basic HTML
            let beskrivning = '';
            if (npc.beskrivning) {
                beskrivning = npc.beskrivning
                    .replace(/### (.*?)$/gm, '<h3>$1</h3>')
                    .replace(/## (.*?)$/gm, '<h2>$1</h2>')
                    .replace(/# (.*?)$/gm, '<h1>$1</h1>')
                    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
                    .replace(/\n\n/g, '</p><p>')
                    .replace(/^(.*)$/gm, '<p>$1</p>');
            }

            // Image path
            const bildUrl = npc.bild ? `./kampanjwiki/assets/images/npcs/${npc.bild}` : null;

            body.innerHTML = `
                ${bildUrl ? `<img src="${bildUrl}" alt="${npc.namn}" class="modal-image-clickable" onclick="event.stopPropagation(); showLightbox('${bildUrl}')" style="width: 200px; height: 200px; object-fit: cover; border-radius: 8px; float: right; margin-left: 1rem; margin-bottom: 1rem; border: 2px solid var(--gold);">` : ''}
                <h2>${npc.namn}</h2>
                ${npc.titel ? `<div class="modal-row">
                    <span class="modal-label">Titel:</span>
                    <span class="modal-value">${npc.titel}</span>
                </div>` : ''}
                ${npc.ras ? `<div class="modal-row">
                    <span class="modal-label">Ras:</span>
                    <span class="modal-value">${npc.ras}</span>
                </div>` : ''}
                ${npc.status ? `<div class="modal-row">
                    <span class="modal-label">Status:</span>
                    <span class="modal-value">${npc.status}</span>
                </div>` : ''}
                ${npc.plats ? `<div class="modal-row">
                    <span class="modal-label">Plats:</span>
                    <span class="modal-value">${npc.plats}</span>
                </div>` : ''}
                ${npc.fraktion ? `<div class="modal-row">
                    <span class="modal-label">Fraktion:</span>
                    <span class="modal-value">${npc.fraktion}</span>
                </div>` : ''}
                ${npc.kapitel ? `<div class="modal-row">
                    <span class="modal-label">Första omnämnande:</span>
                    <span class="modal-value">${npc.kapitel}</span>
                </div>` : ''}
                ${beskrivning ? `<div class="modal-beskrivning">${beskrivning}</div>` : ''}
            `;

            modal.classList.add('active');
        }

        function showPlatsModal(plats) {
            const modal = document.getElementById('modal');
            const body = document.getElementById('modal-body');

            // Convert markdown to basic HTML
            let beskrivning = '';
            if (plats.beskrivning) {
                beskrivning = plats.beskrivning
                    .replace(/### (.*?)$/gm, '<h3>$1</h3>')
                    .replace(/## (.*?)$/gm, '<h2>$1</h2>')
                    .replace(/# (.*?)$/gm, '<h1>$1</h1>')
                    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
                    .replace(/\n\n/g, '</p><p>')
                    .replace(/^(.*)$/gm, '<p>$1</p>');
            }

            body.innerHTML = `
                <h2>${plats.namn}</h2>
                ${plats.typ ? `<div class="modal-row">
                    <span class="modal-label">Typ:</span>
                    <span class="modal-value">${plats.typ}</span>
                </div>` : ''}
                ${plats.region ? `<div class="modal-row">
                    <span class="modal-label">Region:</span>
                    <span class="modal-value">${plats.region}</span>
                </div>` : ''}
                ${beskrivning ? `<div class="modal-beskrivning">${beskrivning}</div>` : ''}
            `;

            modal.classList.add('active');
        }

        function closeModal() {
            document.getElementById('modal').classList.remove('active');
        }

        function showLightbox(imageUrl) {
            const lightbox = document.getElementById('lightbox');
            const lightboxImage = document.getElementById('lightbox-image');
            lightboxImage.src = imageUrl;
            lightbox.classList.add('active');
        }

        function closeLightbox() {
            const lightbox = document.getElementById('lightbox');
            lightbox.classList.remove('active');
        }

        function showSection(sectionId) {
            document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
            document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));

            document.getElementById(sectionId).classList.add('active');
            event.target.classList.add('active');
        }

        // Close modal on outside click
        document.getElementById('modal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeModal();
            }
        });

        // Close lightbox on outside click (handled by onclick in HTML)
        // Prevent image click from closing lightbox
        document.addEventListener('DOMContentLoaded', function() {
            const lightboxImage = document.getElementById('lightbox-image');
            if (lightboxImage) {
                lightboxImage.addEventListener('click', function(e) {
                    e.stopPropagation();
                });
            }
        });

        // Close lightbox with ESC key
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                closeLightbox();
                closeModal();
            }
        });

        // Initialize on load
        window.addEventListener('DOMContentLoaded', init);
    </script>
</body>
</html>
